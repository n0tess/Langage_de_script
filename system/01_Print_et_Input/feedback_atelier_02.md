# Feedback — Atelier 2 (S01 Print et Input, OUTHENIN_Nicolas)

## Respect de la consigne

- `input()` pour le prénom et l'âge ✓
- conversion `int()` ✓
- calcul de l'année avec `date.today().year` ✓
- f-string conforme au corrigé ✓

Validation via isalpha() + isdigit() + exit(1). Approche différente du corrigé (qui boucle). isalpha() refuse les prénoms composés ("Marie-Anne") — c'est restrictif.

---
*Évalué sur le commit `7a5e5f3` (fichier `system/01_Print_et_Input/atelier_02.py`).*
