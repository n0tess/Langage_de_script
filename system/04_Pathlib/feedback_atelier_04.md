# Feedback — S04 Atelier 4 (Décomposer un chemin, OUTHENIN Nicolas)

## Respect de la consigne

Critères attendus : fonction `decomposer(chemin: str) -> tuple[str, str, str]` qui renvoie
(dossier, stem, suffix) via `Path.parent`, `.stem`, `.suffix` + 3 exemples codés en dur (dont `.tar.gz` et sans extension)

Constat sur ton code :

- ✓ fonction `decomposer(chemin)`
- ✓ utilisation de `Path`
- ✓ `.parent`, `.stem`, `.suffix`
- ✓ exemple avec extension multiple `.tar.gz`
- ✓ exemple sans extension (`/etc/hosts`)

---
*Évalué sur le commit `4acf348` (fichier `system/04_Pathlib/atelier_04.py`).*
